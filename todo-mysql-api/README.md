# To Run the project please follow the instruction 

1. Create Scheme with help of file " schemaCreationQueries.txt.txt"

-- all database schema and table schema generating query writing in the above mention file
-- just copy the query and run it into the db

2. To install dependencies before starting the project give command in terminal 

--  npm install 


3. Mention your DB user and password in " .env" file to coonect with database  ( basically we don't push .env file and kept in .gitignore but for run the project i am pushing it and jwt secret is also mention there )


4. To start the project give command 

-- npm start or npm run dev (nodemon run)

5. To testing the API in Postman another file name with " postmanApiCalling.txt" is their.
-- In this file all the methods, URL or end points , json (if needed) and authorization requried or not is mention  
-- with the help from it test all the api's





